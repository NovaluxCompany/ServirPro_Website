---
// AdditionalServices.astro - Servicios adicionales (asesoría y trámites)
import { Image } from 'astro:assets';

// Importar iconos SVG
import asesoriaPersonalizadaIcon from '../../assets/iconos/asesoria_personalizada.svg';
import tramitesRapidosIcon from '../../assets/iconos/tramites_rapidos.svg';
---

<!-- Sección de Servicios -->
<section class="py-16" style="background-color: #DDDDDD;">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-6xl mx-auto">

            <!-- Asesoría Personalizada -->
            <div
                class="flex flex-col md:flex-row items-center justify-center md:justify-start space-y-4 md:space-y-0 md:space-x-6 text-center md:text-left">
                <!-- Icono -->
                <div class="flex-shrink-0">
                    <Image src={asesoriaPersonalizadaIcon} 
                           alt="Asesoría personalizada" 
                           class="w-16 h-16 md:w-20 md:h-20 lg:w-24 lg:h-24" />
                </div>

                <!-- Contenido -->
                <div class="flex-1">
                    <h3 class="text-2xl md:text-3xl font-bold mb-2 text-gray subtitle">Asesoría personalizada</h3>
                    <p class="text-gray-700 text-lg md:text-xl description">Un asesor te guía paso a paso</p>
                </div>
            </div>

            <!-- Trámites Fáciles y Rápidos -->
            <div
                class="flex flex-col md:flex-row items-center justify-center md:justify-start space-y-4 md:space-y-0 md:space-x-6 text-center md:text-left">
                <!-- Icono -->
                <div class="flex-shrink-0">
                    <Image src={tramitesRapidosIcon} 
                           alt="Trámites fáciles y rápidos" 
                           class="w-16 h-16 md:w-20 md:h-20 lg:w-24 lg:h-24" />
                </div>

                <!-- Contenido -->
                <div class="flex-1">
                    <h3 class="text-2xl md:text-3xl font-bold mb-2 text-gray subtitle">Trámites fáciles y rápidos</h3>
                    <p class="text-gray-700 text-lg md:text-xl description">Entre 24 y 48 horas</p>
                </div>
            </div>

        </div>
    </div>
</section>