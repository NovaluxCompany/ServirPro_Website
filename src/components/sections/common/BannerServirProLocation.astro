---
import { Image } from 'astro:assets';

import bannerHealth from '../../../assets/images/health-services/servirpro-banner.webp';
import bannerHealthMobile from '../../../assets/images/health-services/servirpro-banner_mobile.webp';
import bannerTitleHealth from '../../../assets/images/health-services/titulo-franja.webp';
---
<!-- Sección Banner (solo web, ancho completo) -->
<section class="relative w-full bg-[#e4e4e4] py-8 md:py-8 mt-4 lg:pb-0 md:pb-0 hidden md:block">
  <!-- Overlay de título en la parte superior, centrado -->
  <div class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10">
    <Image 
      src={bannerTitleHealth} 
      alt="En ServirPRO Seguros" 
      class="w-[720px] max-w-[85vw] h-auto mx-auto"
      loading="lazy"
      quality={90}
    />
  </div>

  <!-- Imagen principal del banner -->
  <div class="w-full flex justify-center overflow-hidden">
    <Image 
      src={bannerHealth} 
      alt="Banner servicios ServirPRO Seguros" 
      class="w-full max-w-none h-auto"
      loading="lazy"
      quality={90}
    />
  </div>
</section>

<!-- Sección Banner (solo móvil, ancho completo, solo imagen) -->
<section class="relative w-full bg-white md:hidden block">
  <div class="w-full overflow-hidden">
    <Image 
      src={bannerHealthMobile} 
      alt="Banner servicios ServirPRO Seguros móvil" 
      class="w-full h-auto"
      loading="lazy"
      quality={90}
    />
  </div>
</section>
